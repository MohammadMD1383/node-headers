name: Sync Node.js headers

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (no commits/tags/push)'
        required: false
        default: 'false'
      min_major:
        description: 'Lowest major Node version to include'
        required: false
        default: '18'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
